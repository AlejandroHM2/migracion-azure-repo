{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "AppConfig": {
    "SeqUrl": "${SEQ_URL}",
    "ConnectionString": "${DB_CONNECTION_STRING}",
    "RedisCacheUrl": "${REDIS_URL}"
  }
}
